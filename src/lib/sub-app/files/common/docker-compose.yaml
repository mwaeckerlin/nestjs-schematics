version: '3.5'
services:
  backend:
    image: ${DOCKER_REPOSITORY}/<%= name %>
    build:
      context: .
      args:
        DOCKER_REPOSITORY:
    ports:
      - '4000:4000'
    deploy:
      restart_policy:
        condition: on-failure
