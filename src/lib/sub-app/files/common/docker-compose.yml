version: '3.5'
services:
  backend:
    build:
      context: ../..
      dockerfile: <%= path %>/<%= name %>/Dockerfile
    ports:
      - '4000:4000'
    deploy:
      restart_policy:
        condition: on-failure
