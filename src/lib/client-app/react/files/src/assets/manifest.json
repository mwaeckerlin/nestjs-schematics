{
  "short_name": "<%= name.replace(/-[a-z]/, (g) => '.' + g[1].toUpperCase()).replace(/^[a-z]/, (g) => g.toUpperCase()) %>",
  "name": "<%= name.replace(/-[a-z]/, (g) => '.' + g[1].toUpperCase()).replace(/^[a-z]/, (g) => g.toUpperCase()) %>",
  "description": "<%= description %>",
  "icons": [
    {
      "src": "icon.svg",
      "type": "image/svg"
    }
  ],
  "start_url": "/",
  "display": "fullscreen",
  "theme_color": "#20295b",
  "background_color": "#ffffff",
  "orientation": "any"
}
